# Vaultwarden - Environment Variables
#
# SECRETS - Add these to Infisical at /services/vaultwarden/
# ─────────────────────────────────────────────────────────
VAULTWARDEN_ADMIN_TOKEN=         # Required: Admin panel access token

# Optional SMTP credentials (for email notifications)
VAULTWARDEN_SMTP_USERNAME=       # SMTP username
VAULTWARDEN_SMTP_PASSWORD=       # SMTP password

# CONFIGURATION - Set in Komodo stack settings
# ─────────────────────────────────────────────
ENV=dev                          # dev or prod
BASE_DOMAIN=thebozic.com         # Base domain for Traefik routing
TZ=America/Toronto               # Timezone

# Optional overrides
SIGNUPS_ALLOWED=false            # Allow public signups
INVITATIONS_ALLOWED=true         # Allow admin invitations
SHOW_PASSWORD_HINT=false         # Show password hints

# SMTP configuration (optional)
SMTP_HOST=smtp.mailgun.org       # SMTP server
SMTP_FROM=vaultwarden@homelab.thebozic.com
SMTP_PORT=587
SMTP_SECURITY=starttls           # starttls, force_tls, or off
